{
  "name": "oration-spin-hub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "node scripts/dev.js",
    "build": "npm run build --prefix frontend",
    "seed": "npm run seed --prefix backend"
  },
  "devDependencies": {}
}
